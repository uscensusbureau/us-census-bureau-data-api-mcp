{
  "name": "census-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "docker-build": "docker build -t census-api .",
    "prepare": "npm run build",
    "test": "vitest --silent=false",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.25.32"
  },
  "devDependencies": {
    "@types/node": "^22.15.23",
    "shx": "^0.4.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
